<!DOCTYPE html>
<html lang="en">
  
<head>
    <meta charset="utf-8">
    <title>우리집 앞마당 모니터링</title>
    
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="apple-mobile-web-app-capable" content="yes">    
    
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <link href="css/bootstrap-responsive.min.css" rel="stylesheet">
    
    <link href="http://fonts.googleapis.com/css?family=Open+Sans:400italic,600italic,400,600" rel="stylesheet">
    <link href="css/font-awesome.css" rel="stylesheet">
    
    <link href="css/style.css" rel="stylesheet">
    
    
    <link href="js/guidely/guidely.css" rel="stylesheet"> 

    <!-- Le HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
  </head>

<body>

<?php include 'header.php'?>
    
    
<div class="main">
	<div class="main-inner">
	    <div class="container">
	      <div class="row">	      	
	      	
	      	<div class="span12">
	      		<div id="target-1" class="widget">
	      			<div class="widget-content">
			      		<h1>우리집 앞마당</h1>
			      		<p>우리집 앞마당은 도시농업에 기여하기 위해 출시한 제품입니다.<br>바쁜 현대인들에게 저렴한 가격과 편리한 기능으로 도심 속 힐링을 선사해줍니다.<br>
                           
                        </p>	
		      		</div> <!-- /widget-content -->
	      		</div> <!-- /widget -->
      		</div> <!-- /span12 -->
      		
	      	<div class="span6">
	      		<div id="target-2" class="widget">
	      			<div class="widget-content">
			      		<h1>모니터링</h1>
			      		<p>우리집 앞마당으로 수집한 작물 데이터들을 한 눈에 확인할 수 있습니다.<br>
                            일별 온습도와 토양수분, 월별 온습도와 토양수분를 그래프로 보여줍니다.<br>
                            더불어 현재 상태를 수치화하여 명료하게 제공하며 일정 달력을 통해 작물을 관리할 수 있습니다. </p>	
		      		</div> <!-- /widget-content -->
	      		</div> <!-- /widget -->
      		</div> <!-- /span6 -->
      		 	
	      	<div class="span6">
	      		<div id="target-3" class="widget">
	      			<div class="widget-content">
			      		<h1>커뮤니티</h1>
			      		<p>우리집 앞마당을 사용하는 이용자들끼리 정보를 나눌 수 있도록 커뮤니티를 제공합니다.<br>
                            초보자라 작물 키우는게 부담스러우시다구요? 고수분들이 작물 키우는 여러 팁을 가르쳐주실 겁니다.<br>
                            커뮤니티를 통해 각자 키우는 작물에 대한 정보와 일상을 공유해보세요!</p>	
		      		</div> <!-- /widget-content -->
	      		</div> <!-- /widget -->
      		</div> <!-- /span6 -->
      		
	      	<div class="span4">
	      		<div id="target-4" class="widget">
	      			<div class="widget-content">
			      		<h1>찾아보기</h1>
			      		<p>커뮤니티 뿐만이 아니라 우리집 앞마당에서도 작물에 대한 정보를 제공합니다! 필요에 따라 정보를 찾아보세요!</p>			
		      		</div> <!-- /widget-content -->
	      		</div> <!-- /widget -->
      		</div> <!-- /span4 -->
	      	
	      	<div class="span4">
	      		<div class="widget">
	      			<div class="widget-content">
                        <h1>찾아보기</h1>
			      		<p>작물의 정보는 크게 잎채소, 열매채소, 뿌리채소, 식량작물, 허브로 나누었습니다.
                            각기 분류한 작물에 대해서 비슷한 작물들끼리 2차 분류했기 때문에 정보찾기가 훨신 수월하실겁니다!</p>
		      		</div> <!-- /widget-content -->
	      		</div> <!-- /widget -->
      		</div> <!-- /span4 -->
      		 	
	      	<div class="span4">
	      		<div class="widget">
	      			<div class="widget-content">
                        <h1>찾아보기</h1>
			      		<p>작물 정보와 더불어 작물 재배, 제품 사용 팁에 대해서도 알려드립니다!<br><br> </p>	
		      		</div> <!-- /widget-content -->
	      		</div> <!-- /widget -->
      		</div> <!-- /span4 -->

              <div id="target-5" class="span3">
                <div class="widget">    
                    <div class="widget-content">    
                      <h1>고객센터</h1>
                        <p>제품과 웹 관제 시스템의 공지사항을 확인하실 수 있습니다.</p>     
                    </div> <!-- /widget-content --> 
                </div> <!-- /widget --> 
            </div> <!-- /span3 -->  
            
            <div class="span3">
                <div class="widget">
                    <div class="widget-content">    
                        <h1>고객센터</h1>
                        <p>자주 묻는 질문들을 통해 제품을 사용하시면서 또는 웹 관제 시스템을 이용하면서 생기는 궁금증들을 해결해드립니다.</p>   
                    </div> <!-- /widget-content -->
                </div> <!-- /widget -->
            </div> <!-- /span3 -->

              <div class="span3">
                <div class="widget">
                    <div class="widget-content">
                        <h1>고객센터</h1>
                        <p>자주 묻는 질문에 내가 궁금한 질문이 없으신가요? 1:1문의하기를 통해 질문해보세요!<br>
                            관리자가 이메일 또는 전화번호로 답변을 보내드립니다.</p>   
                    </div> <!-- /widget-content --> 
                </div> <!-- /widget -->
            </div> <!-- /span3 -->
            
            <div id="target-6" class="span3">
                <div class="widget">    
                    <div class="widget-content">    
                      <h1>기타</h1>
                        <p>도시농업을 위해 알짜배기들의 기능들을 한 곳에 담은 우리집 앞마당!
                            매력적이지 않나요?<br> 
                            코로나와 지친 일상 속에서 도심 힐링인 우리집 앞마당과 함께 해보세요!</p>     
                    </div> <!-- /widget-content --> 
                </div> <!-- /widget --> 
            </div> <!-- /span3 -->

	      	
             
	      	
	      </div> <!-- /row -->
	    </div> <!-- /container -->
	</div> <!-- /main-inner -->   
</div> <!-- /main -->
    


<?php include 'footer.php'?>
<!-- /footer -->
    

<!-- Le javascript
================================================== -->
<!-- Placed at the end of the document so the pages load faster -->
<script src="js/jquery-1.7.2.min.js"></script>

<script src="js/bootstrap.js"></script>
<script src="js/base.js"></script>

<script src="js/guidely/guidely.min.js"></script>

<script>
var guidely = (function() {
    return {
        _guides: [],
        _defaults: {
            showOnStart: true,
            welcome: true,
            welcomeTitle: "우리집 앞마당에 오신걸 환영합니다!",
            welcomeText: '우리 제품과 사이트의 간단한 둘러보기를 시작하려면 클릭하세요!<br>여기에서는 더 쉽게 사용할 수 있도록 중요한 기능과 팁을 알려드립니다.',
            overlay: true,
            startTrigger: true,
            escClose: true,
            keyNav: true,
            debug: false
        },
        _options: {},
        init: function(config) {
            var that, options;
            that = this;
            that._options = $.extend(that._defaults, config);
            if (that._guides.length < 1) {
                that._log('No guides available.');
                return false;
            }
            that._append();
            that._createTopAnchor();
            that.close();
            if (that._options.startTrigger) {
                that._createStartTrigger();
            }
            if (that._options.showOnStart) {
                if (that._options.welcome) {
                    that.welcome();
                } else {
                    that.start();
                }
            }
        },
        start: function() {
            var that = this;
            that.close();
            that._scrollToTop();
            that.showNumbers();
            that.show(1);
            that.showOverlay();
            that._log(that._guides);
            that._reposition();
            $(document).bind('keyup.guidely', function(e) {
                if (that._options.escClose) {
                    if (e.keyCode == 27) {
                        that.close();
                    }
                }
                if (that._options.keyNav) {
                    if (e.keyCode == 37) {
                        that.prev();
                    }
                    if (e.keyCode == 39) {
                        that.next();
                    }
                }
            });
            $(window).bind('resize.guidely', function() {
                that._reposition();
            });
        },
        welcome: function() {
            var that, content, popup, pad, controls, close, overlay, startBtn, noBtn;
            that = this;
            that.close();
            content = $('<div>', {
                'id': 'guide-welcome',
                'class': 'guidely-guide'
            }).appendTo('body');
            popup = $('<div>', {
                'class': 'guidely-popup'
            }).appendTo(content);
            pad = $('<div>', {
                'class': 'guidely-guide-pad'
            }).appendTo(popup);
            pad.append('<h4>' + that._options.welcomeTitle + '</h4>');
            pad.append(that._options.welcomeText);
            controls = $('<div>', {
                'class': 'guidely-controls'
            }).appendTo(popup);
            content.css({
                'position': 'absolute',
                'top': '75px',
                'left': '50%',
                'margin-left': '-' + content.outerWidth() / 2 + 'px'
            });
            close = $('<a>', {
                'href': 'javascript:;',
                'class': 'guidely-close-trigger',
                'html': 'x',
                'click': function() {
                    that.close();
                }
            }).appendTo(content);
            this.showOverlay();
            startBtn = $('<button>', {
                text: "알겠습니다",
                click: function() {
                    that.close();
                    that.start();
                }
            }).appendTo(controls);
            noBtn = $('<button>', {
                text: '괜찮습니다',
                click: function() {
                    that.close();
                }
            }).appendTo(controls);
            content.show();
            that._scrollToTop(function() {
                startBtn.focus();
            });
            $(document).bind('keyup.guidely', function(e) {
                if (e.keyCode == 27) {
                    that.close();
                }
            });
        },
        show: function(guideId) {
            var guide = $('#guidely-guide-' + guideId);
            var number = $('#guidely-number-' + guideId);
            this._resetZIndex();
            number.css({
                'z-index': '20001'
            });
            guide.show().css({
                'z-index': '20000'
            });
            guide.find('.guidely-controls button').focus();
        },
        next: function() {
            var that, current, next, id;
            that = this;
            current = $('.guidely-guide:visible');
            next = current.nextAll('.guidely-guide:not(#guide-welcome)').eq(0);
            if (next.length > 0) {
                current.hide();
                id = next.attr('id').split('-')[2];
                that.show(id);
            }
        },
        prev: function() {
            var that, current, prev, id;
            that = this;
            current = $('.guidely-guide:visible');
            prev = current.prevAll('.guidely-guide:first');
            if (prev.length > 0) {
                current.hide();
                id = prev.attr('id').split('-')[2];
                that.show(id);
            }
        },
        add: function(config) {
            var options = {
                anchor: 'top-left'
            };
            options = $.extend(options, config);
            if (options.text === '' || options.text === undefined) {
                this._log('Guide id ' + options.attachTo + ' cannot be blank.');
                return false;
            }
            this._guides.push(options);
        },
        close: function() {
            this.hideNumbers();
            this.hideGuides();
            this.hideOverlay();
            $(document).unbind('keyup.guidely');
            $(window).unbind('resize.guidely');
        },
        hideGuides: function() {
            $('.guidely-guide').hide();
        },
        showNumbers: function() {
            $('.guidely-number').fadeIn('slow');
        },
        hideNumbers: function() {
            $('.guidely-number').hide();
        },
        showOverlay: function() {
            if (!this._options.overlay) {
                return false;
            }
            var overlay = $('.guidely-overlay');
            if (!overlay.length) {
                $('<div>', {
                    'class': 'guidely-overlay'
                }).appendTo('body');
            } else {
                overlay.show();
            }
        },
        hideOverlay: function() {
            $('.guidely-overlay').hide();
        },
        _createNumber: function(id, options) {
            var that, number, coords, displayId;
            that = this;
            coords = this._getAnchorCoords(options);
            displayId = id;
            number = $('<div>', {
                'id': 'guidely-number-' + id,
                'class': 'guidely-number',
                'html': '<span>' + displayId + '</span>'
            });
            number.css({
                'top': coords[0],
                'left': coords[1]
            });
            number.bind('click', function() {
                if ($('#guidely-guide-' + id).is(':visible')) {
                    return false;
                }
                that.showOverlay();
                that.hideGuides();
                that.show(id);
            });
            number.appendTo('body').show();
        },
        _createGuide: function(id, options) {
            var that, content, popup, pad, close, controls, coords, number;
            that = this;
            coords = this._getCoords(options);
            number = $('#guidely-number-' + id);
            content = $('<div>', {
                'id': 'guidely-guide-' + id,
                'class': 'guidely-guide'
            }).appendTo('body');
            close = $('<a>', {
                'href': 'javascript:;',
                'class': 'guidely-close-trigger',
                'html': 'x',
                'click': function() {
                    that.close();
                }
            }).appendTo(content);
            popup = $('<div>', {
                'class': 'guidely-popup'
            }).appendTo(content);
            pad = $('<div>', {
                'class': 'guidely-guide-pad'
            }).appendTo(popup);
            if (options.title !== '' && options.title !== undefined) {
                pad.append('<h4>' + options.title + '</h4>');
            }
            pad.append(options.text);
            controls = $('<div>', {
                'class': 'guidely-controls'
            }).appendTo(popup);
            if (this._guides.length == id) {
                this._createDoneButton().prependTo(controls);
            } else {
                this._createNextButton().appendTo(controls);
            }
            this._setGuidePosition(number, content);
        },
        _setGuidePosition: function(number, content) {
            var offset, left, docWidth, docHeight;
            offset = number.offset();
            docWidth = $(document).width();
            docHeight = $(document).height();
            if (content.outerWidth() + offset.left > docWidth) {
                left = (offset.left - content.outerWidth()) + 30;
                content.addClass('guidely-anchor-right');
            } else {
                left = (offset.left + number.outerWidth()) - 30;
                content.removeClass('guidely-anchor-right');
            }
            content.css({
                'position': 'absolute',
                'top': offset.top + 15,
                'left': left
            });
        },
        _createStartTrigger: function() {
            var that = this;
            $('<a>', {
                'href': 'javascript:;',
                'class': 'guidely-start-trigger',
                'html': 'Start Tour',
                'click': function(e) {
                    e.preventDefault();
                    if (that._options.welcome) {
                        that.welcome(that._options.title, that._options.text);
                    } else {
                        that.start();
                    }
                }
            }).appendTo('body');
        },
        _getCoords: function(options) {
            var el = $(options.attachTo);
            return {
                width: el.outerWidth(),
                height: el.outerHeight(),
                top: el.offset().top,
                left: el.offset().left
            };
        },
        _getAnchorCoords: function(options) {
            var coords, offsetMap, anchor, numberOffset;
            coords = this._getCoords(options);
            anchor = options.anchor.replace('-', '_');
            numberOffset = 45 / 2;
            offsetMap = {
                top_left: [coords.top - numberOffset, coords.left - numberOffset],
                top_right: [coords.top - numberOffset, (coords.left + coords.width) - numberOffset],
                top_middle: [coords.top - numberOffset, coords.left + (coords.width / 2) - numberOffset],
                middle_left: [coords.top + (coords.height / 2) - numberOffset, coords.left - numberOffset],
                middle_middle: [coords.top + (coords.height / 2) - numberOffset, coords.left + (coords.width / 2) - numberOffset],
                middle_right: [coords.top + (coords.height / 2) - numberOffset, coords.left + coords.width - numberOffset],
                bottom_right: [coords.top + coords.height - numberOffset, (coords.left + coords.width) - numberOffset],
                bottom_middle: [coords.top + coords.height - numberOffset, coords.left + (coords.width / 2) - numberOffset],
                bottom_left: [coords.top + coords.height - numberOffset, coords.left - numberOffset]
            };
            return offsetMap[anchor];
        },
        _createNextButton: function() {
            var that = this;
            return $('<button>', {
                text: 'Next',
                click: function() {
                    that.next();
                }
            });
        },
        _createDoneButton: function() {
            var that = this;
            return $('<button>', {
                text: 'Done',
                click: function(e) {
                    e.preventDefault();
                    that.close();
                }
            });
        },
        _resetZIndex: function() {
            $('.guidely-number').css({
                'z-index': '10002'
            });
            $('.guidely-guide').css({
                'z-index': '10001'
            });
        },
        _append: function() {
            for (var i = 0, l = this._guides.length; i < l; i++) {
                var di = i + 1;
                this._createNumber(di, this._guides[i]);
                this._createGuide(di, this._guides[i]);
            }
        },
        _reposition: function() {
            for (var i = 0, l = this._guides.length; i < l; i++) {
                var display_id, coords, number, content;
                display_id = i + 1;
                coords = this._getAnchorCoords(this._guides[i]);
                number = $('#guidely-number-' + display_id);
                number.css({
                    'top': coords[0],
                    'left': coords[1]
                });
                content = $('#guidely-guide-' + display_id);
                this._setGuidePosition(number, content);
            }
        },
        _scrollToTop: function(callback) {
            var target_offset = $("#guidely-top").offset();
            var target_top = target_offset.top;
            $('html, body').animate({
                scrollTop: target_top
            }, 500, function() {
                if (typeof callback === 'function') {
                    callback();
                }
            });
        },
        _log: function(msg) {
            if (typeof window.console != 'undefined' && typeof window.console.log != 'undefined' && this._options.debug) {
                console.log(msg);
            }
        },
        _createTopAnchor: function() {
            $('<div>', {
                id: 'guidely-top'
            }).prependTo('body');
        }
    };
})(this);


$(function () {
	
	guidely.add ({
		attachTo: '#target-1'
		, anchor: 'top-left'
		, title: '우리집 앞마당'
		, text: '반자동화된 시스템과 웹 관제 시스템을 통해 편리한 작물재배를 도와줍니다.'
	});

	guidely.add ({
		attachTo: '#target-2'
		, anchor: 'top-middle'
		, title: '모니터링'
		, text: '우리집 앞마당을 통해 수집한 정보들을 수치화시켜 한 페이지에 보여줍니다.'    
	});

    guidely.add ({
		attachTo: '#target-3'
		, anchor: 'top-middle'
        , title: '커뮤니티'
		, text: '우리집 앞마당 제품을 사용하는 이용자들끼리 커뮤니티를 통해 정보를 나눌 수 있습니다.'   
	});
	
	guidely.add ({
		attachTo: '#target-4'
		, anchor: 'top-right'
		, title: '찾아보기'
		, text: '우리집 앞마당에서 키우기 좋은 작물들을 추천해주고 여러가지의 팁들을 제공합니다.'
	});
	
	guidely.add ({
		attachTo: '#target-5'
		, anchor: 'top-right'
		, title: '고객센터'
		, text: '웹 관제 시스템과 제품 관련 공지사항들을 확인할 수 있으며 질문에 대한 답변을 받을 수 있습니다.'
	});

    guidely.add ({
		attachTo: '#target-6'
		, anchor: 'top-right'
		, title: '기타'
		, text: '매력적인 우리집 앞마당 한 번 사용해보고 싶지 않으신가요? 지금 당장 회원가입 하세요!'
	});
	
    
	guidely.init ({ welcome: true, startTrigger: false });

});


</script>
  </body>

</html>
